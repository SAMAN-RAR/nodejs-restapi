# Database
MONGODB_URI=mongodb+srv://username:password@cluster0.nkbt7xy.mongodb.net/messages?appName=Cluster0

# Server
PORT=8080
NODE_ENV=development

# JWT Secret (for production, use a strong secret)
JWT_SECRET=your_jwt_secret_key_here
